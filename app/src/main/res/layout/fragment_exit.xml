<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    android:background="@color/Blanco"
    android:gravity="center"
    android:orientation="vertical">

    <ImageView
        android:id="@+id/ivSure"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:background="@drawable/ic_gorro_chef"
        android:contentDescription="Final de la partida"
        android:scaleType="center" />

    <TextView
        android:id="@+id/otraPartida"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginBottom="5dp"
        android:text="Vas a salir sin guardar\n      ¿Estas segur@?"
        android:textColor="@color/Negro"
        android:textSize="20sp"
        android:textStyle="bold" />


    <Button
        android:id="@+id/btnSi"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@drawable/btn_animado"
        android:contentDescription="Si"
        android:scaleType="center"
        android:text="Si"
        android:textColor="@color/Blanco" />

    <Button
        android:id="@+id/btnNo"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:background="@drawable/btn_animado"
        android:contentDescription="No"
        android:scaleType="center"
        android:text="No"
        android:textColor="@color/Blanco" />
</LinearLayout>

